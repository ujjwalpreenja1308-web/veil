[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --timeout-keep-alive 120 --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
