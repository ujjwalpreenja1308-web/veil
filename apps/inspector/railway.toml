[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port 8000 --timeout-keep-alive 120 --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
